import React from 'react';

const Loading = () => {
    return (
        <div>
            <h2>Loading ...........</h2>
            <p className=' progress'></p>
        </div>
    );
};

export default Loading;